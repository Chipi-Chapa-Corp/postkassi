# Postkassi
GNOME-first email client

# Development
```bash
# Install dependencies
npm i

# Install system dependencies
sudo dnf install atk at-spi2-atk gtk3 glib2 pango cairo gdk-pixbuf2 alsa-lib nss nspr mesa-libgbm
sudo ldconfig

# Run development mode
npm run dev
```
