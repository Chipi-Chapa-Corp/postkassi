import { createTheme } from "@renderer/utility/styled";

export const lightTheme = createTheme({
	colors: {
		background: "#ffffff",
		itemBackground: "#f5f5f5",
		itemHoverBackground: "#e0e0e0",
	},
});
